# Legacy Proposals

This directory contains historical proposal material only. Do not create new feature proposals here.

For every new module or cross-module behavior, create an OpenSpec change with:

```text
npm run spec:new -- <verb-noun-change-id>
```

Use `openspec/changes/<change-id>/` for the proposal, design, tasks, and delta specifications.
