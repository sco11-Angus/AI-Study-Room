# Flask 业务/算法后端依赖（§10.3）
flask==3.0.*
flasgger==0.9.*          # Swagger / OpenAPI 自动文档
flask-cors==4.0.*
flask-sock==0.7.*        # WebSocket 告警推送 (§7.3)
opencv-python==4.9.*
numpy>=2.0
scipy>=1.11              # legacy YOLOv5 checkpoint compatibility
tqdm>=4.66               # legacy YOLOv5 checkpoint compatibility
ultralytics==8.*         # YOLOv8n (§5.3, §6.1) → 自带 torch>=2.0
dlib==19.24.*            # 68 点人脸模型 (§4.3, §7.2)
SQLAlchemy==2.0.*
PyMySQL==1.1.*             # MySQL/MariaDB driver for DATABASE_URI=mysql+pymysql://...
mysql-connector-python>=9.0 # MySQL driver for DATABASE_URI=mysql+mysqlconnector://...
python-dotenv==1.0.*       # Optional .env loading helper
torch>=2.0                 # PyTorch（由 ultralytics 自动安装，显式声明确保版本）
torch-vggish-yamnet>=1.0  # PyTorch YAMNet + VGGish 预训练模型（~14MB 权重自动下载）
requests==2.31.*         # 钉钉 Webhook (§7.4)
gunicorn==21.*           # 部署 (§11)
funasr-onnx>=0.1         # SenseVoiceSmall ONNX 推理
funasr>=1.3               # SenseVoiceSmall 模型转 ONNX 所需（首次导出后缓存）
modelscope>=1.38          # SenseVoiceSmall 从 ModelScope 下载模型
onnxscript>=0.7           # ONNX 导出脚本
jieba>=0.42               # 中文分词（funasr 依赖）
